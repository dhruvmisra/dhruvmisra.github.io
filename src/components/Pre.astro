---
import CopyButton from "@/components/CodeBlock/CopyButton.tsx";
export interface Props {
    raw: string;
}

const { raw, ...props } = Astro.props;
---

<pre {...props}>
    <CopyButton text={raw} client:load />
    <slot /> 
</pre>
